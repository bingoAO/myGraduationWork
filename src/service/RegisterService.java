package service;

import java.util.List;

public interface RegisterService {
	
	//to complete the user register
public boolean registerUser(List<Object> params);
public boolean isUserExist(List<Object> params);
}
